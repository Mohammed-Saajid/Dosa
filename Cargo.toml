[package]
name = "dosa"
version = "0.1.1"
edition = "2024"
license = "Apache-2.0"
description = "A Project Manager CLI Utility"

[dependencies]
dialoguer = {version = "0.11", features = ["fuzzy-select"] }
anyhow = "1"
serde = { version = "1.0", features = ["derive"] }
toml = "1.1.2"
dirs = "6.0.0"
clap = { version = "4.6.5", features = ["derive"] }
